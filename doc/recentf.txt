*recentf.txt*  Recent file viewer for Vim

Author:  Greg Anders <greg@gpanders.com>
Repo:    https://github.com/gpanders/vim-recentf
License: Same terms as Vim itself (see |license|)

INTRODUCTION                                             *recentf* *:Recentf*

Vim provides a built-in method for viewing your recent file history with the
|:oldfiles| command. This plugin seeks to complement and improve the built-in
command by listing recent files in a separate, navigable buffer, allowing
filtering of files in the recent files list, and keeping the oldfiles list
up-to-date during your Vim session.

To view your recent files, simply use
>
        :Recentf
<
The Recent Files buffer is just a plain old Vim buffer that you can |:sort|,
filter, search, and modify as you like. Modifying this buffer will not change
the |v:oldfiles| variable that recentf uses.

==============================================================================
MAPPINGS                                                   *recentf-mappings*

Buffer-local ~
    q           Close the buffer
    <CR>        Open file at cursor
    gr          Reload the buffer (removes any changes you've made)

==============================================================================
SETTINGS                                                   *recentf-settings*

						*g:recentf_blacklist*
g:recentf_blacklist		list (default [])
		A list of Vim |pattern|s to filter from the recent
		files list.

		Example: The following will ignore all Vim help docs,
		any Fugitive buffers, and any files underneath a .git
		directory (such as COMMIT_MSG files)
>
			let g:recentf_blacklist = [
			      \ 'vim/.*doc/.\+\.txt$',
			      \ '^fugitive://',
			      \ '/\.git/.\+'
			      \ ]
<

						*g:recentf_ignore_unreadable*
g:recentf_ignore_unreadable	boolean (default 1)
		If |TRUE|, filter entries from the recent files list for which
		|filereadable()| returns |FALSE|.

						*g:recentf_use_wildignore*
g:recentf_use_wildignore	boolean (default 1)
		If |TRUE|, files matching patterns in 'wildignore' will be
		filtered from the recent files list.

 vim:tw=78:ts=8:noet:ft=help:norl:
